<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.overloading">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_sell"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Watch"
            android:label="@string/clock"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".Detail"
            android:label="@string/detail"
            android:parentActivityName=".AddressDetail" />
        <activity
            android:name=".AddressDetail"
            android:label="@string/address_detail"
            android:parentActivityName=".Address" />
        <activity
            android:name=".Menu"
            android:label="@string/menu"
            android:parentActivityName=".Sell" />
        <activity
            android:name=".Address"
            android:label="@string/address"
            android:parentActivityName=".Sell" />
        <activity
            android:name=".Store"
            android:label="@string/store"
            android:parentActivityName=".Sell" />
        <activity
            android:name=".Sell"
            android:label="@string/sell"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>